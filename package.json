{
  "name": "micdrop-monorepo",
  "author": "Lonestone",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "examples/*",
    "doc"
  ],
  "scripts": {
    "build": "pnpm build:server && pnpm build:client && concurrently -n \"ai-sdk,cartesia,elevenlabs,gladia,mistral,openai,react\" -c \"blue,green,yellow,magenta,cyan,red,green\" \"pnpm --filter ai-sdk build\" \"pnpm --filter cartesia build\" \"pnpm --filter elevenlabs build\" \"pnpm --filter gladia build\" \"pnpm --filter mistral build\" \"pnpm --filter openai build\" \"pnpm --filter react build\"",
    "build:server": "pnpm --filter server build",
    "build:client": "pnpm --filter client build",
    "build:demo": "pnpm --filter demo-server build && pnpm --filter demo-client build",
    "build:doc": "pnpm --filter doc build",
    "clean": "pnpm -r clean",
    "dev:main": "concurrently -n \"server,client\" -c \"blue,green\" \"pnpm --filter server dev\" \"pnpm --filter client dev\"",
    "dev:demo": "concurrently -n \"demo-server,demo-client\" -c \"blue,yellow\" \"pnpm --filter demo-server dev\" \"pnpm --filter demo-client dev\"",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "prettier": "^3.0.0"
  },
  "packageManager": "pnpm@8.15.4"
}
