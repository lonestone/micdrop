---
/**
 * The stack of sections below the hero. Each section carries its own surface
 * and its own vertical padding, so the rhythm lives with the section rather
 * than in a wrapper that spaces everything identically.
 */
---

<div class="flex w-full flex-col">
  <slot />
</div>
