# Umami analytics (only loaded in production builds). Leave empty to disable.
PUBLIC_UMAMI_WEBSITE_ID=
# Optional: override the Umami script URL (defaults to the shared instance).
PUBLIC_UMAMI_SCRIPT_URL=
